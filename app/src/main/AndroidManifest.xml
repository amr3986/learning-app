<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.funlearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalActivity"></activity>
        <activity android:name=".FifthActivity5" />
        <activity android:name=".forthActivity4" />
        <activity android:name=".ThirdActivity3" />
        <activity android:name=".SecondActivity2" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ZeroActivity0">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>